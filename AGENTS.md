# Kimi Agent Instructions

## Document Storage Convention

When creating new documents (Word, text files, reports, etc.) in this project directory,
default to saving them under the `kimidocx/` folder unless the user explicitly specifies
a different location.

